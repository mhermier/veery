
import "veery/compiler/abstract_lang/visitor" for AbstractVisitor;

class ComposedVisitor is AbstractVisitor {
  construct new(lhs, rhs) {
    _lhs = lhs
    _rhs = rhs
  }

  visit(node) { node.accept(_rhs).accept(_lhs) }
}
